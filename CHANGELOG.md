# Changelog

## 1.2 — строгая развязка Telemt и Nginx на TCP/443

- для `Telemt=443` Nginx теперь принципиально генерируется только с `listen 127.0.0.1:9443`; публичный listener 443 создаётся исключительно в отдельном режиме `direct443`, когда Telemt использует другой порт;
- после генерации выполняется строгая проверка `nginx.conf`: любая директива Nginx `listen ...:443` при `Telemt=443` немедленно останавливает установку до запуска контейнеров;
- doctor отдельно подтверждает отсутствие дублирующего Nginx-listener TCP/443;
- устранён нестабильный запуск worker под UID 101: Nginx снова использует root внутри изолированного контейнера, но сохраняет read-only filesystem, `no-new-privileges`, `cap_drop: ALL` и только `SETUID/SETGID`; для отдельного Nginx на 443 условно добавляется `NET_BIND_SERVICE`;
- ошибка `chown(/tmp/client_body, 101) failed (Operation not permitted)` больше не возникает, поскольку Nginx не меняет владельца временных каталогов на UID 101.

## 1.1 — безопасный общий TCP/443

- исправлен режим `Telemt на TCP/443 + HTTPS-заглушка`: публичный порт всегда принадлежит только Telemt, а обычный браузерный HTTPS передаётся во внутренний Nginx на `127.0.0.1:9443`;
- внутренний порт Nginx больше не предлагается и не отображается как публичный адрес; внешний сайт открывается как `https://домен/`, а MTProto-ссылка использует тот же `server` и `port=443`;
- MTProto FIX V3 теперь исключает loopback до маркировки и SYN-лимита, поэтому локальный relay, API и healthcheck не получают случайный TCP reset;
- добавлена строгая проверка владельца TCP/443: установка останавливается, если порт слушает не контейнер Telemt;
- doctor проверяет loopback-only привязку Nginx, прохождение сайта через Telemt и наличие loopback bypass в обеих цепочках FIX;
- в ранней сборке v1.1 Nginx запускал worker от пользователя `nginx`; этот вариант заменён в v1.2 из-за несовместимости с `chown()` временных каталогов на некоторых Docker-хостах;
- каталог HTML получает режим `755`, а `index.html` — `644`, поэтому непривилегированный worker Nginx может читать публичную страницу без доступа на запись;
- healthcheck Nginx ускорен до 5 секунд, а установщик ожидает готовность до 90 секунд и при реальной ошибке показывает журнал контейнера;
- терминальные итоги и README явно показывают маршрут `Internet:443 → Telemt → 127.0.0.1:9443 → Nginx`.

## 1.0 — monitoring refresh

- README полностью переработан как руководство по сценариям: сравнение двух режимов, быстрый старт, ожидаемые изменения, команды, безопасность и диагностика без повторов;
- первый экран переработан в выбор двух независимых сценариев: полная система Telemt или только MTProto FIX V3 для существующего прокси;
- добавлен автономный режим FIX-only: показывает слушающие TCP-порты, спрашивает порт/список и устанавливает только iptables/kmod, V3-скрипт и systemd unit; не запрашивает и не изменяет домен, secret, ad-tag, Docker, Telemt, GeoBlock и мониторинг;
- терминальный интерфейс разделён на этапы «Настройка → Установка → Проверка → Готово»; шаги нумеруются внутри этапа и завершаются названием операции и временем;
- начальный GeoBlock запускается неблокирующим systemd-вызовом: быстрый результат ожидается до минуты, затем загрузка продолжает работать в фоне без длительного удержания одного D-Bus-соединения;
- интегрирован опциональный MTProto FIX V3 iptables из MTPROTO_FIX_By_MEKO: мастер установки спрашивает порт или список портов, по умолчанию использует порт Telemt; добавлены идемпотентные цепочки, systemd-восстановление и меню `mtproto fix` со status/install/apply/remove;
- разрешённые V3 SYN-пакеты возвращаются в основной firewall установщика, поэтому правила FIX не обходят GeoBlock; doctor, ports, firewall, credentials, backup, restore и uninstall учитывают модуль;
- исправлен запуск Telemt на привилегированных портах 1–1023, включая TCP/443: для них контейнер условно стартует с UID 0, но сохраняет read-only filesystem, `no-new-privileges` и только capability `NET_BIND_SERVICE`; на портах 1024+ используется непривилегированный пользователь официального образа;
- GeoIP Exporter теперь фиксирует проверенную версию `geoip2==5.3.0`, не проверяет обновления pip и не выводит неуместное для изолированного Docker build предупреждение о запуске pip от root;
- исправлено ложное 15-минутное ожидание APT из-за постоянно работающего `unattended-upgrade-shutdown --wait-for-sig`: теперь учитываются только процессы, действительно удерживающие lock-файлы APT/dpkg;
- добавлен безопасный плановый перезапуск только контейнера Telemt через systemd timer: 30 минут по умолчанию, варианты 1/3/6/12/24 часа и отключение; после рестарта выполняется healthcheck, а остановленный администратором контейнер не запускается;
- APT/dpkg lock от `unattended-upgrades` теперь ожидается до 15 минут с PID, названием процесса и обратным отсчётом; все apt-команды используют `DPkg::Lock::Timeout`, lock-файлы не удаляются;
- node_exporter переведён на стандартную схему systemd: пользователь `node_exporter`, бинарник `/usr/bin/node_exporter`, unit `node_exporter.service`; добавлена миграция с раннего `telemt-node-exporter.service`;
- loopback (`lo`, включая `127.0.0.1`) теперь всегда исключён из GeoBlock и внешнего `DROP` метрик; локальный healthcheck node_exporter ограничен пятью секундами вместо многоминутного ожидания;
- исправлена контрольная сумма node_exporter v1.5.0 (`…acbb`); проверка дополнительно сверяется с официальным `sha256sums.txt` и показывает ожидаемое/полученное значение при ошибке;
- три страницы-заглушки полностью переработаны в premium-стиле и не содержат персональных имён;
- добавлен node_exporter v1.5.0 с проверкой SHA-256, отдельным systemd unit и портом 9100;
- Telemt, GeoIP и node_exporter доступны удалённой Grafana только по точному IPv4 allow-list;
- `mtproto credentials` теперь повторяет полный итог установки: ключи, Telegram-ссылки, сайт, endpoints и карту портов;
- backup, restore, doctor, ports и uninstall учитывают node_exporter.

## 1.0 — Initial release

- цветной пошаговый мастер установки;
- каждый шаг показывает итоговое время, а после подтверждения установки — живой счётчик каждые 5 секунд;
- добавлены `mtproto help`, алиас `mtproto secrets` и безопасный `mtproto geoblock pause` с автоматическим возвратом через 5 минут;
- деинсталлятор полностью очищает runtime, секреты, backups, сертификат, контейнеры, правила, units и legacy-каталоги проекта;
- EE/TLS выбран рекомендуемым режимом по умолчанию; DD оставлен для совместимости и сравнительной проверки;
- добавлены `mtproto sponsor` и `mtproto client-debug` для проверки ad-tag, Middle Proxy и попыток подключения iOS;
- мастер теперь объясняет обязательный шаг `Set promotion` и условия отображения Proxy Sponsor;
- опциональная публичная HTTPS-заглушка на TCP/443: сайт открывается как `https://домен/`, а также через порт Telemt;
- безопасный повторный запуск непосредственно из `/opt/telemt` без самокопирования;
- проверка конфликтов всех портов, временное открытие TCP/80 для Certbot и команда `mtproto ports`;
- исправлено преждевременное отображение итогов портов до настройки заглушки и мониторинга;
- исправлена YAML-запись `tmpfs` для Nginx и GeoIP exporter в Docker Compose;
- Fail2ban теперь проходит проверку конфигурации и ожидание управляющего сокета;
- права `config.toml` и cache автоматически согласуются с non-root UID/GID официального образа Telemt;
- runtime/cache Telemt перенесён в tmpfs без UID-зависимого bind mount; конфигурация совместима с Docker userns-remap;
- итог выдаёт сайт, парные `tg://proxy` и `https://t.me/proxy` ссылки для каждого режима и локальные URL мониторинга/API;
- пропуск территорий без IPdeny-файла и безопасный повтор GeoBlock после сетевых ошибок;
- порядок регистрации: IP/домен и порт → Proxy Secret → ad-tag;
- видимый результат каждого этапа и готовые Telegram proxy-ссылки;
- закрытая памятка `/opt/telemt/INSTALLATION-SUMMARY.txt` и команда `mtproto credentials`;
- установка всего runtime в `/opt/telemt`;
- hardened Telemt в Docker Compose;
- DD/EE, закрытые API и метрики;
- IPv4/IPv6 GeoBlock и Fail2ban;
- Nginx HTTPS-заглушка с тремя HTML5-шаблонами;
- Let's Encrypt и self-signed fallback;
- диагностика, backup/restore, update с откатом и uninstall;
- отдельные сервисные сценарии внутри `/opt/telemt`.
- документированный перенос на другой сервер с сохранением домена, порта и секрета.
